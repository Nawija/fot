export type MenuBurgerProps = {
  handleShowMenu: () => void;
  showMenu: boolean;
};
export type HandleCloseMenuProps = {
  handleCloseMenu: () => void;
};
export type ClassNameProps = {
  className: string;
};
